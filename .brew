#!/bin/bash

# Make sure we’re using the latest Homebrew
brew update

# Upgrade any already-installed formulae
brew upgrade

# Install GNU core utilities (those that come with OS X are outdated)
brew install coreutils
echo "Don’t forget to add $(brew --prefix coreutils)/libexec/gnubin to \$PATH."

# Install GNU `find`, `locate`, `updatedb`, and `xargs`, g-prefixed
brew install findutils

# Install Bash 4 and link
brew install bash
sudo rm /bin/bash
sudo ln -s /usr/local/bin/bash /bin/bash

# Install wget with IRI support
brew install wget --enable-iri

# Install more recent versions of some OS X tools
brew tap homebrew/dupes
brew install homebrew/dupes/grep
brew tap josegonzalez/homebrew-php

# gcc/g++ compilers
brew install gcc49
sudo ln -s /usr/local/bin/gcc-4.9 /usr/local/bin/gcc
sudo ln -s /usr/local/bin/g++-4.9 /usr/local/bin/g++

# Databases
brew install redis
brew install mongodb
wget -P ~/Downloads https://github.com/jimbojsb/launchrocket/releases/download/v0.6/LaunchRocket.prefPane.zip

# Caskfile manager
brew tap phinze/cask
brew install brew-cask

# Install everything else
brew install imagemagick
brew install graphicsmagick
brew install ctags
brew install ack
brew install git
brew install rename
brew install tree
brew install gist
brew install youtube-dl
brew install speedtest-cli

# Install linters dependencies
npm install -g jshint
sudo pip install cpplint
brew install cppcheck
npm install -g csslint
gem install rubocop
npm install -g coffeelint
gem install scss-lint
brew install cabal-install
echo "for install shellcheck follow this instructions: https://sublime.wbond.net/packages/SublimeLinter-shellcheck"
read -p "Press enter when shellceck is installed..."

# wbench
gem install wbench
brew install chromedriver

# Remove outdated versions from the cellar
brew cleanup